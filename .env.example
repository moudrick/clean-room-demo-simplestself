GH_ORG=featureflag-extensiveconsumer-demo-org
LD_PROJECT_KEY=featureflag-extensiveconsumer-demo-key
GH_RESET_TOKEN=
GH_DEMO_TOKEN=
LD_RESET_TOKEN=
LD_DEMO_TOKEN=
# Optional; refresh or recreate applies this only to checkout in Production.
LD_PROBE_DETAILED_EVENTS=false
# Optional; true makes recreate, refresh, and destroy refuse before any preflight.
# Set true while a campaign sandbox holds evidence that must never be deleted.
CAMPAIGN_LOCK=false
